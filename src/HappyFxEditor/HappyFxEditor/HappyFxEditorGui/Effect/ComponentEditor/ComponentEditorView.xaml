<UserControl x:Class="HappyFxEditorApp.Effect.ComponentEditor.ComponentEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ce="clr-namespace:HappyFxEditorApp.Effect.ComponentEditor"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.Resources>
        <DataTemplate x:Key="behaviourTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ce:ComponentBehaviourView Grid.Column="0" />          
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <ListBox ItemsSource="{Binding SelectedComponent.Behaviours}" ItemTemplate="{StaticResource behaviourTemplate}"
             SelectedValue="{Binding SelectedBehaviour}" HorizontalContentAlignment="Stretch"/>
    
</UserControl>